# User-facing GraphQL schema served by Yoga.

type Dog {
  name: String!
}

# FIXME Dummy mutation to get started
type Mutation {
  createDog(name: String!): Dog
}

# FIXME Dummy query to get started
type Query {
  dogs: [Dog]!
}
